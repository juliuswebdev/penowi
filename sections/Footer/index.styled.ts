import styled from 'styled-components'

const StyledFooterMessenger = styled.div`

`

const StyledFooterMessengerTest = styled.div`
    position: fixed;
    top: 0;
    right: 0;
    z-index: 999;
`

export {
    StyledFooterMessenger,
    StyledFooterMessengerTest
}