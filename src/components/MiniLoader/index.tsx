import { StyledMiniLoader } from './index.styled'

function MiniLoader() : JSX.Element {
    return (
        <StyledMiniLoader>
            <span>Loading...</span>
        </StyledMiniLoader>
    )
}

export default MiniLoader;